// x => x.id === '45'
// res = sessionStorage.getItem('data-news');
// console.log(res);
// res = JSON.parse(res);
// content = res.find(x => x.id === '45').title.rendered;
// content = res.find(x => x.id === '45').title.rendered;
// $("#content-detail article").html(content);
// $("#content-detail h1").text(title);
//
